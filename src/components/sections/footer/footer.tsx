import React, { ReactElement } from 'react';

import { Container } from '../../shared';

export const Footer = (): ReactElement => (
  <footer className="c-footer">
    <Container>
      <small>&copy; 2020 - React</small>
    </Container>
  </footer>
);
